Database structure
classes:
	videos:{
		title,
		description,
		class,
		user,
		date,
		comments,
		view counts,
		likes
	}
	notes:{
		title,
		description,
		class,
		user,
		date,
		comments,
		view counts,
		likes
	}

Joy:

payment:
	token done;
	charge:need
tab video

Huahua:
database
upload, delete & view notes&&videos
